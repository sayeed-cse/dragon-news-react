import React from 'react';
import { Navigate } from 'react-router';

const Home = () => {
    return (
        <div>
            return <Navigate to='/category/0'></Navigate>
        </div>
    );
};

export default Home;
